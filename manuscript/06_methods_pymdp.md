# On-policy student: pymdp sophisticated inference {#sec:methods_pymdp}

<!-- sheaf-track:prose -->

**The on-policy student as sophisticated inference (planning horizon).** Under our thesis, on-policy distillation is the student policy acting to minimise expected free energy: it generates its own observations through rollouts and is scored against a privileged target. This section documents the canonical **pymdp full TMaze sophisticated-inference validation profile** ([@fig:tmaze_schematic]) as the executable minimal model of that on-policy student, with SI search horizon {{si_tmaze_planning_horizon}}, rollout length {{si_tmaze_steps}}, and Agent `policy_len = {{si_tmaze_policy_len}}`. Sophisticated inference — beliefs about beliefs over the planning horizon — is precisely the structure invoked by self-distillation methods that condition a teacher on the student's own verified traces [@zhao2026opsd; @liu2026sdpg]; an agent that rolls out and then minimises expected free energy is the active-sampling counterpart of the student rollouts in on-policy distillation [@agarwal2024gkd]. The discrete-state active-inference framing follows finite-POMDP treatments, comparisons with RL, scaling discussions, and tutorial literature [@dacosta2020discrete; @sajid2021demystified; @smith2022tutorial; @parr2022active; @tschantz2020scaling_active_inference; @tschantz2020rl_active_inference], with the expected-free-energy decomposition into epistemic and pragmatic terms supplying the information-gain-versus-reward split that distillation losses recapitulate [@millidge2020efe; @friston2019generalised; @champion2024efe_unification; @sajid2021bayesian_design; @devries2025efe_planning_vi]. The implementation anchor is pymdp's `TMaze`, `Agent`, `si_policy_search`, and `rollout` APIs [@pymdp2024]. The configured profile is `{{pymdp_profile}}`; the canonical planner is `{{pymdp_planner}}`.

**The same situation in two frameworks.** To make the correspondence concrete rather than analogical, we solve one scenario — two-state reward-location inference under an informative cue — in both the active-inference and the standard machine-learning idiom. On the active-inference side, the teacher is the exact Bayesian posterior $p(s\mid o)$, the unique minimiser of variational free energy. On the machine-learning side, a student categorical policy with logits $\theta$ (a softmax policy) is trained by gradient descent on the reverse-KL on-policy-distillation loss $D_{\mathrm{KL}}(\pi_S\Vert\pi_T)$ using `jax` automatic differentiation. The two procedures converge to the *same* distribution: the ML-distilled student reproduces the active-inference posterior to within {{parallel_max_abs_difference:.1e}} (the artifact's frameworks-agree flag is {{parallel_frameworks_agree}}), and its variational free energy reaches {{parallel_student_free_energy:.3f}} nats, matching the evidence bound $-\ln p(o)={{parallel_neg_log_evidence:.3f}}$ nats. The jax reverse-KL distillation gradient *is* the variational free-energy gradient; the equivalence is executed, not asserted [@levine2018rlinference; @penaloza2026pidistill].

The generative process has {{si_tmaze_location_state_count}} location states/actions, {{si_tmaze_reward_location_state_count}} reward-location states, and {{si_tmaze_observation_modality_count}} observation modalities (location, outcome, cue). The cue modality carries the privileged information of the distillation correspondence: it is the hint, verified trace, or feedback channel available in training but not guaranteed at inference, and `cue_validity` is the strength of that privilege [@friston2013life; @kirchhoff2018markov; @vapnik2009lupi; @lopezpaz2016unifying; @cai2024privileged_pomdp]. That makes the asymmetry operational: the teacher has privileged sensory access, whereas the on-policy student must act to sample the channel and is then evaluated on the trajectory it actually induced. The model/value audit in `output/data/si_tmaze_model_matrices.json` records {{si_tmaze_matrix_shape_summary}}, dependencies, preferences, deterministic D priors, reward condition {{si_tmaze_reward_condition}}, and cue validity {{si_tmaze_cue_validity}} ([@fig:si_tmaze_model_matrices]). Per-step trace records include $q_\pi$, marginal first-action probabilities, selected action names, modality-specific observations, belief entropy, and SI tree metadata — the on-policy trajectory the student writes and is then scored against.

Graph-world artifacts are deterministic extension outputs declared in `tracks.yaml` `extension_tracks.graph_world`. For the reference workflow, see [@sec:intro_motivation]; measured rollouts appear in [@sec:results_si_tmaze].

Mean belief entropy across recorded timesteps: {{si_tmaze_mean_belief_entropy_formatted}} nats. Because the cue is informative, the student that acts to observe it drives its posterior entropy down — the epistemic value of seeking privileged information made quantitative. The initial $q_\pi$ first-action marginal assigns probability {{si_tmaze_first_action_cue_probability:.3f}} to the cue-directed action, which is the canonical first-action argmax under the configured SI search: the on-policy student elects, from its own beliefs, to sample the privileged channel first.

The comparison artifact `output/data/si_policy_comparison.json` runs the canonical SI planner alongside a vanilla pymdp planner as validation rows only; the vanilla planner is marked `{{si_policy_comparison_vanilla_role}}` and never replaces the canonical summary. It records {{si_policy_comparison_run_count}} deterministic comparison rows, complete-grid flag {{si_policy_comparison_complete_grid}}, and {{si_policy_comparison_goal_reached_count}} goal-reaching rows under the same full TMaze transition model. We read this contrast as the difference between an agent that minimises expected free energy on-policy and a myopic baseline. That contrast is the active-inference image of the induced-distribution issue from behavioral cloning, interactive imitation learning, sequence generation, policy distillation, and LLM distillation: the learner must be trained on states it actually causes, not only on teacher-generated states [@pomerleau1989alvinn; @ross2010efficient_imitation; @ross2011dagger; @sun2017aggrevated; @bengio2015scheduled; @arora2022exposure; @rohatgi2025next_token_barrier; @pozzi2025exposure_distill; @hinton2015distilling; @kim2016sequence_kd; @rusu2016policy_distillation; @czarnecki2019distilling_policy; @agarwal2024gkd].

Agent construction and rollout diagnostics are audited in `output/reports/pymdp_runtime_diagnostics.json`: {{pymdp_runtime_construction_count}} constructions, {{pymdp_runtime_known_warning_count}} known third-party JAX static-array warnings, {{si_tree_known_max_node_warning_count}} known SI tree max-node diagnostics, and {{pymdp_runtime_unexpected_warning_count}} unexpected warnings. Policy posterior evidence is written separately to `output/data/pymdp_policy_posterior_grid.json` with {{pymdp_policy_posterior_row_count}} rows and normalized-posterior flag {{pymdp_policy_posteriors_normalized}}.

The graph-world extension is deterministic: `simulate_si_graph_world.py` writes `si_graph_world_summary.json` and `si_graph_world_trace.json` for a four-node graph-world path. The regenerated summary reports {{si_graph_world_node_count}} nodes, {{si_graph_world_steps}} steps, and goal-reached flag {{si_graph_world_goal_reached}}. The topology-trace extension records {{si_graph_world_topology_trace_count}} topology traces with agreement flag {{si_graph_world_topology_traces_agree}}. As with the analytical toy, these are a minimal-model demonstration of the on-policy-distillation/active-inference correspondence — claims are limited to these pymdp models and artifacts, not to production LLM systems.

<!-- sheaf-track:formalism -->

Given generative matrices $A,B,C,D$, pymdp computes state beliefs $q(s)$ and policy posterior rows $q_\pi$ inside `rollout`. The canonical SI Agent uses `policy_len = {{si_tmaze_policy_len}}`, while `si_policy_search` supplies the effective search horizon $H =$ {{si_tmaze_planning_horizon}} (logged with `num_policies = {{si_tmaze_num_policies}}` and tree metadata in the SI summary artifact; see [@sec:results_si_tmaze]).

The default harness records belief entropy per step and $q_\pi$ first-action probabilities from the sophisticated-inference rollout. Vanilla planning is retained only in `output/data/si_policy_comparison.json` as comparison evidence, not as a manuscript co-primary track.

<!-- sheaf-track:pymdp -->

SI artifacts (summary, trace, optional JSONL log) record the canonical {{pymdp_profile}} rollout: {{si_tmaze_steps}} rollout transitions, {{si_tmaze_rollout_timestep_count}} recorded timesteps, {{si_tmaze_observation_modality_count}} observation modalities, {{si_tmaze_q_pi_row_count}} $q_\pi$ rows, {{si_tmaze_action_probability_row_count}} marginal first-action probability rows, and SI tree availability flag {{si_tree_available}}.

<!-- sheaf-track:interop -->

The `interop` fragment treats the GNN files, JSON views, and ontology bindings as a round-trip contract rather than parallel documentation. That places this manuscript's GNN use in the broader effort to make active-inference models diagrammatically specified and machine-checkable [@gnn2023; @koudahl2023synthetic]. `output/data/interop_roundtrip_report.json` records {{interop_check_count}} deterministic checks and reports lossless round-trip status `{{interop_all_lossless}}`; the manuscript only claims losslessness from that generated flag.

The stricter lint artifacts are adjacent evidence, not new model claims: `output/data/gnn_roundtrip_report.json`, `output/reports/gnn_lint_report.json`, `output/data/ontology_alias_index.json`, and `output/data/ontology_profile_matrix.json` must agree before the interop row passes. A missing GNN variable, duplicate ontology alias, dropped JSON field, shape diff, or dtype diff is therefore a validation failure before rendering.

<!-- sheaf-track:visualization -->

![Schematic of pymdp's full TMaze generative process: {{si_tmaze_location_state_count}} location states/actions, {{si_tmaze_reward_location_state_count}} reward-location states, three observation modalities, cue validity {{si_tmaze_cue_validity}}, and SI search horizon {{si_tmaze_planning_horizon}}. The diagram lays out the hidden states, observations, and controllable transitions that define the teacher's task, including the cue arm whose validity determines how informative the epistemic action is. It fixes the world model in which the active-inference teacher is computed and, in turn, the task structure an on-policy student must operate within to be distilled faithfully.](../output/figures/tmaze_schematic.png){#fig:tmaze_schematic width=85% fig-alt="Schematic of pymdp's full TMaze generative process with five locations/actions, two reward-location states, three observation modalities, and SI search horizon {{si_tmaze_planning_horizon}}."}

![Full TMaze generative-model matrix and value audit. Left: the labeled A (likelihood) and B (transition) factors with shapes {{si_tmaze_matrix_shape_summary}} and their state dependencies, alongside C preferences and D priors. Right: A, B, and D normalization checks confirming each conditional distribution sums to unit probability mass (from `output/data/si_tmaze_model_matrices.json`). The audit exposes the exact parameters that generate the teacher's behavior and verifies they are valid probabilities, so the active-inference policy being distilled rests on a well-formed generative model rather than an unchecked numerical artifact.](../output/figures/si_tmaze_model_matrices.png){#fig:si_tmaze_model_matrices width=92% fig-alt="Matrix summary figure for the pymdp full TMaze model. Left panel lists labeled A and B factors, shapes, and dependencies; right panel shows A, B, and D normalization checks around unit probability mass."}

![One scenario solved in two frameworks. A standard machine-learning loop (jax automatic differentiation on the reverse-KL distillation loss) drives a student policy to the active-inference exact posterior $p(s\mid o)$: the per-step reverse KL decays to zero (left) and the distilled student matches the posterior to within {{parallel_max_abs_difference:.1e}} (right), its variational free energy reaching {{parallel_student_free_energy:.3f}} nats -- the evidence bound $-\ln p(o) = {{parallel_neg_log_evidence:.3f}}$. The reverse-KL distillation gradient is the variational free-energy gradient, so the correspondence is executed rather than asserted. Source: `output/data/firstprinciples/parallel_demo.json`.](../output/figures/parallel_convergence.png){#fig:parallel_convergence width=95% fig-alt="Two-panel figure. Left: the reverse-KL on-policy-distillation loss versus gradient step on a logarithmic axis, decaying toward zero as a jax-autodiff student is trained. Right: grouped bars comparing the active-inference exact posterior with the machine-learning-distilled student over the hidden states; the bars coincide."}

<!-- sheaf-track:gnn -->

See `gnn/si_tmaze.gnn.md` for a GNN view of the full pymdp TMaze hidden-state factors, three observation modalities, $q_\pi$ first-action posterior, belief entropy, and SI tree evidence with ontology bindings.

<!-- sheaf-track:ontology -->

### Ontology bindings

- `belief_entropy` → **BeliefEntropy**
- `first_action_prob` → **PolicyPosterior**
- `loc` → **HiddenState**
- `obs_cue` → **ObservationLikelihood**
- `obs_location` → **ObservationLikelihood**
- `obs_outcome` → **ObservationLikelihood**
- `q_pi` → **PolicyPosterior**
- `reward_loc` → **HiddenState**
- `si_tree_nodes` → **PolicyPosterior**

