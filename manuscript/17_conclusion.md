# Conclusion {#sec:conclusion}

Analytical oracles ([@sec:methods_analytical]), pymdp rollouts ([@sec:results_si_tmaze]), and sheaf composition ([@sec:methods_sheaf]) all share one auditable manuscript contract: measured artifacts hydrate {{composed_section_count}} composed sections, [@sec:sheaf_coverage] reports binding state, and strict compose validation blocks gray matrix cells before PDF rendering. This is what casts on-policy distillation as active inference: a single declared generative model is held accountable to its own measured outputs at every stage.

The full TMaze harness runs under canonical `{{pymdp_planner}}` planning with config hash `{{pymdp_config_hash}}`, and a sweep RMSE of {{sweep_rmse_mi}} nats summarizes analytical–empirical agreement on the toy coupling grid. [@sec:results_invariants] merges the analytical and simulation gates, while [@sec:discussion_outlook] states scope and extensions. Throughout, our scientific claims remain confined to the declared models and their artifacts; they are not empirical statements about biological agents or production language models.
