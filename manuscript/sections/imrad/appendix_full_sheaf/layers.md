This appendix binds the generated `layers` report as a full-track witness.
The rendered body is produced by the `layers_report` renderer from the live
track registry, manifest binding matrix, coverage artifacts, semantic
restriction certificate, producer maps, and render log, so the appendix carries
the same sheaf-layer tables as the methods section through the common renderer
dispatch.
