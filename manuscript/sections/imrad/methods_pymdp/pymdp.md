SI artifacts (summary, trace, optional JSONL log) record the canonical {{pymdp_profile}} rollout: {{si_tmaze_steps}} rollout transitions, {{si_tmaze_rollout_timestep_count}} recorded timesteps, {{si_tmaze_observation_modality_count}} observation modalities, {{si_tmaze_q_pi_row_count}} $q_\pi$ rows, {{si_tmaze_action_probability_row_count}} marginal first-action probability rows, and SI tree availability flag {{si_tree_available}}.
