# Free-energy decomposition {#sec:results_free_energy}

<!-- sheaf-track:prose -->

Read as a distillation-objective landscape, the variational free energy of the student against the reward-tilted teacher target is evaluated along the same $\lambda$ grid used for the MI sweep ([@fig:free_energy_curve]), where $\lambda$ couples the teacher's privileged variable to the answer and thus sets how much teacher–student mutual information the objective must absorb. [@fig:free_energy_curve] separates two objects that are easy to conflate. Against the *entangled* target $p_\lambda$ — the analogue of the teacher policy $\pi_T(y\mid x, I)$ that carries privileged information $I$ — the entangled posterior $q_\lambda$ is evaluated against its own normalized target, so $F(q_\lambda;p_\lambda)=0$ to numerical tolerance (maximum absolute value {{free_energy_exact_target_max_abs:.1e}} nats). That zero is not an absence of structure; it is the finite self-distillation limit [@zhao2026opsd] where the tractable student family has exactly recovered the target it is fit against and the reverse-KL loss vanishes.

The decomposition implemented in `src/analytical/decomposition.py` then splits that zero into per-stream marginal free energies, a coupling-cost term, a coupling-prior term, and a total-correlation gain — the same epistemic/pragmatic ledger that expected-free-energy methods balance in active inference [@friston2010fep; @parr2022active; @millidge2020efe; @champion2024efe_unification; @sajid2021bayesian_design]. For the symmetric toy with uniform marginals, the coupling-prior term equals $-I(\lambda)$ and exactly cancels the total-correlation gain $+I(\lambda)$; the merged invariant suite checks this cancellation directly ({{invariants_passed}}/{{invariants_total}} pass). This is the precise sense in which teacher–student mutual information is the slack between the coupled target and the factorized family for this model.

The nonzero curve in [@fig:free_energy_curve] is the free-energy gap against the *mean-field* independent prior $q_0$, which plays the role of the mode-covering, forward-KL SFT baseline [@hinton2015distilling] that ignores the privileged coupling. Its minimum at $\lambda={{free_energy_argmin_lambda}}$ occurs where the entangled posterior coincides with the factorized mean-field product; any $\lambda>0$ raises the gap as coupling pulls the posterior away from that independent prior. At the configured sweep maximum the gap reaches {{free_energy_mean_field_gap_max:.3f}} nats and equals mutual information up to {{free_energy_gap_equals_mi_max_abs:.1e}} nats, so the rising branch is the information gap the on-policy reverse-KL objective must close [@gu2024minillm; @agarwal2024gkd]. As the privileged variable becomes more diagnostic, a student that cannot condition on $I$ pays an increasing cost, exactly the regime where on-policy self-distillation toward a teacher conditioned on verified traces is designed to operate [@liu2026sdpg; @hubotter2026sdpo].

Saturation MI (grid maximum on the measured $\lambda$ sweep): {{ising_mi_saturation}} nats. This is the largest coupling attained on the measured $\lambda$ grid, not the model's ceiling: as $\lambda\to\infty$ the mutual information saturates toward $\log 2 \approx 0.693$ nats ([@eq:mutual_information]), which is the information the privileged target can carry beyond the student's reach in this binary toy. As throughout, the claim is limited to this analytical toy as a faithful minimal demonstration of the correspondence, not an assertion about production LLMs.

### Energy decompositions: VFE and EFE {#sec:energy_decompositions}

To make the correspondence between the reverse-KL distillation loss and variational free energy (VFE) explicit, we evaluate the two energy ledgers of active inference on the same minimal model and report them in [@fig:energy_decomposition]. VFE admits the standard complexity-minus-accuracy reading [@friston2010fep; @parr2022active]: $F = \underbrace{D_{\mathrm{KL}}[q(s)\,\|\,p(s)]}_{\text{complexity}} - \underbrace{\mathbb{E}_q[\ln p(o\mid s)]}_{\text{accuracy term}}$, equivalently the negative log-evidence offset by the posterior-to-true-posterior gap. In the prior-evaluated panel, the complexity term is {{energy_complexity:.3f}} nats because $q(s)=p(s)$, so the KL cost of moving off the prior is zero. The accuracy term is {{energy_accuracy:.3f}} nats because it is an expected log likelihood and is negative under this observation model; therefore the plotted VFE is $0 - ({{energy_accuracy:.3f}})$, the positive surprisal term behind the reported log-evidence bound {{energy_log_evidence:.3f}} nats. The point is not that energy disappears, but that the cost comes entirely from insufficient accuracy when the student remains at the prior. In distillation language, that is the unlearned teacher signal: minimizing $F$ tightens the reverse KL between the tractable student $\pi_S(y\mid x)$ and the privileged target $\pi_T(y\mid x, I)$ up to the model-evidence constant $-\ln p(o)$ that is invariant to the student parameters [@levine2018rlinference; @dacosta2020discrete]. This correspondence is an algebraic objective identity inside the declared finite categorical family — the variational reading of the divergence in the sense of the free-energy-principle mathematical review [@buckley2017mathreview] — so a shared objective form fixes the optimum here but does not carry over to the optimization dynamics or scaling of large-model distillation, where the same reverse-KL objective behaves in support-, parameterisation-, and optimization-dependent ways rather than as a universal law [@wu2024rethinking_kl_kd].

Expected free energy (EFE) extends this ledger forward over the student's own rollouts, which is where the *on-policy* character of distillation enters: the student generates the very observations it is then scored against, so the relevant energy is an expectation over self-generated trajectories rather than a fixed teacher-forced corpus [@devries2025efe_planning_vi; @liu2026sdpg; @hubotter2026sdpo]. EFE splits two equivalent ways. The risk-plus-ambiguity reading assigns {{efe_risk:.3f}} nats to risk (the divergence of predicted from preferred outcomes — the reward-tilting term that biases rollouts toward the verified target) and {{efe_ambiguity:.3f}} nats to ambiguity (the expected observation entropy under the generative mapping). The epistemic-plus-pragmatic reading assigns {{efe_epistemic:.3f}} nats to the epistemic (information-gain) drive and {{efe_pragmatic:.3f}} nats to the pragmatic (goal-seeking) drive. The pragmatic term is the active-inference image of reward-tilting in on-policy distillation, while the epistemic term is the active-sampling pressure that makes on-policy rollouts close the exposure-bias gap the off-policy SFT baseline leaves open [@qwen2025technical_report; @thinkingmachines2025opd; @zhao2026opsd].

The full variational- and expected-free-energy decomposition is tabulated in the supplement ([@sec:appendix_full_sheaf]).

The table makes the thesis quantitative on one model: VFE is the static distillation objective (complexity {{energy_complexity:.3f}} traded against accuracy {{energy_accuracy:.3f}}), and EFE is its on-policy extension, where the pragmatic drive {{efe_pragmatic:.3f}} is reward-tilting and the epistemic drive {{efe_epistemic:.3f}} is active sampling over the student's own rollouts. As elsewhere, these are nats from a faithful minimal-model demonstration, not production measurements; the literature-reported empirics are reported separately.

#### Active rollout selection: expected free energy chooses where to distill {#sec:active_selection}

The energy ledgers above evaluate EFE on a fixed model; the *active* step is to let expected free energy choose which states the student rolls out on, which is precisely what the word on-policy names and what the correspondence map otherwise leaves to action selection rather than to the realized-rollout loss. [@fig:active_selection_landscape] makes that selection explicit on a minimal T-maze-style menu of data-collection policies. A student that matches the privileged teacher posterior on a policy's own observation distribution keeps an expected residual uncertainty about the reward-relevant latent equal to that channel's conditional entropy, so the student–teacher gap a policy can close is exactly its epistemic value [@parr2022active]: across the {{active_selection_sweep_points}}-point cue-validity sweep, epistemic value and residual gap sum to the prior entropy {{active_selection_prior_entropy_nats:.3f}} nats at every point. Minimising expected free energy over the {{active_selection_policy_count}} candidate policies therefore selects the cue-visiting policy, whose informative rollouts drive the residual gap to {{active_selection_cue_residual:.1e}} nats; an ablated selector that keeps only the pragmatic reward-tilting term commits to an arm and leaves {{active_selection_pragmatic_residual:.3f}} nats of teacher signal unrecoverable, and blinding the cue reopens the gap. In this finite toy the epistemic term of expected free energy is thus not decoration but the quantity that fixes whether on-policy distillation can become exact; as elsewhere the claim is exact only for this declared model and is not a statement about production distillation.

The active-selection result is robust along three axes, each prototype-checked before promotion. First, **generality**: the identity is not an artefact of the binary toy -- it holds across {{active_general_channel_count}} observation channels with three- and four-valued latents to {{active_general_max_identity_residual:.1e}} nats, while a wrong-measure ablation (weighting the residual by a uniform rather than the predicted observation distribution) breaks it by {{active_general_wrong_measure_gap:.3f}} nats, confirming the identity measures the genuine coupled quantity rather than holding vacuously. Second, **sequential depth**: a single step cannot show why an agent visits the cue *first*, since epistemic value is instrumental to a later goal. With a declared cue step-cost of {{sequential_cue_step_cost:.2f}} nats, a myopic one-step planner prefers to commit immediately while a two-step sophisticated-inference planner still prefers the cue ({{sequential_cue_policy_efe:.3f}} versus {{sequential_commit_policy_efe:.3f}} nats of summed policy expected free energy) -- the planning horizon is what creates the preference. Extended over a horizon, the cue's instrumental value scales: the cue/commit expected-free-energy gap grows by exactly {{sequential_per_step_instrumental_value:.3f}} nats per remaining exploit step (break-even horizon {{sequential_break_even_horizon:.2f}}), so a one-step planner commits but every horizon of two or more cues, with the cost held in an analytically-derived window rather than tuned. Third, and most striking, the closed-form result **quantitatively predicts a measured observable of the project's pymdp simulation** ([@fig:si_bridge_match]): the analytical residual at the environment's own cue validity ({{si_bridge_cue_validity:.2f}}) equals the sophisticated-inference agent's measured post-cue belief entropy ({{si_bridge_post_cue_entropy:.3f}} nats) to {{si_bridge_match_abs:.1e}} nats, the agent visits the cue before any arm, and the match holds only at the environment's actual cue validity -- a prediction bound to the model, not a fit. The prediction is not confined to that single post-cue point: the closed-form running-Bayesian belief entropy tracks the agent's measured belief entropy across the whole rollout -- the prior, the post-cue plateau, and the resolved-to-zero tail -- to {{si_bridge_max_trajectory_error:.1e}} nats ([@fig:si_belief_entropy_trajectory]), with the non-trivial agreement at the cue transition itself; a wrong cue validity or a shuffled observation order breaks the match. The bridge is stated at the level of observable behaviour and belief entropy (pymdp does not expose its internal expected-free-energy terms), and remains exact only for these declared finite models. The algebraic *structure* of the underlying complement identity is additionally machine-checked: a sorry-free Lean skeleton certifies $I(o;r) + E_o[H(r\mid o)] = H(r)$ over the integers, with a strictly-positive-residual control transferring strictly less than the prior entropy ([@sec:methods_lean]); the real-valued entropy form remains the two-route numerical witness above.

<!-- sheaf-track:visualization -->

![Free-energy gap created by privileged teacher--student coupling across {{param_sweep_grid_points}} sweep points. The exact entangled target $p_\lambda$ gives $F(q_\lambda;p_\lambda)=0$ to numerical tolerance (max absolute value {{free_energy_exact_target_max_abs:.1e}} nats) because the posterior is evaluated against its own normalized target. The independent mean-field baseline $q_0$ instead pays $D_{\mathrm{KL}}(q_\lambda\|q_0)$, rising to {{free_energy_mean_field_gap_max:.3f}} nats; in this symmetric toy that gap equals mutual information $I(\lambda)$ up to {{free_energy_gap_equals_mi_max_abs:.1e}} nats. The right panel makes the cancellation explicit: the exact-target coupling-prior term and total-correlation gain cancel, while the mean-field student keeps the information gap that on-policy distillation is meant to close.](../output/figures/free_energy_curve.png){#fig:free_energy_curve width=90% fig-alt="Two-panel free-energy gap figure. The left panel plots the exact entangled target free energy as a dashed zero line, the independent mean-field student gap as a rising solid curve, and mutual information as a dotted comparison curve. The right panel shows the lambda-max decomposition where the coupling-prior term cancels total correlation for the exact target while the mean-field gap remains positive."}

![Energy-based decompositions for the categorical generative model, showing the two quantities active inference uses to score perception and action. Left: variational free energy split into complexity {{energy_complexity:.3f}} minus the accuracy term {{energy_accuracy:.3f}} nats (bounding log-evidence {{energy_log_evidence:.3f}}). Complexity is zero here because the evaluated student belief equals the prior, so $D_{\mathrm{KL}}(q\|p(s))=0$; the VFE is therefore the negative of the negative log-likelihood accuracy term, not an unexplained zero. Right: expected free energy split into risk {{efe_risk:.3f}} plus ambiguity {{efe_ambiguity:.3f}} nats, equivalently subtracting epistemic {{efe_epistemic:.3f}} and pragmatic {{efe_pragmatic:.3f}} value. Sign key: risk and ambiguity are penalties (positive bars worsen $G$), epistemic and pragmatic are values (positive bars improve $G$), under the exact identity $G = risk + ambiguity = -(epistemic + pragmatic)$ as decomposed in `output/data/firstprinciples/energy_demo.json`; all bars are deterministic closed-form evaluations in nats. Reading distillation through this lens, the complexity/accuracy and risk/ambiguity terms make explicit that an on-policy student is simultaneously matching the teacher's outputs (accuracy/pragmatic) and reducing its own uncertainty about unvisited states (epistemic).](../output/figures/energy_decomposition.png){#fig:energy_decomposition width=95% fig-alt="Two-panel energy-based decomposition figure. The left panel shows the variational free energy split into complexity and accuracy bars with the net free energy; the right panel shows the expected free energy split into risk, ambiguity, epistemic, and pragmatic bars, generated from the first-principles energy artifact."}

![The active half of the correspondence: expected free energy chooses where the student collects rollouts. Left: across {{active_selection_sweep_points}} cue-validity settings the epistemic value (information gain) rises and the residual distillation gap falls, and at every point their sum is the prior entropy H(r)={{active_selection_prior_entropy_nats:.3f}} nats — the exact identity that the gap a policy can close equals its epistemic value. Right: the expected-free-energy decomposition for the {{active_selection_policy_count}} canonical data-collection policies; minimising expected free energy selects the cue, whose rollouts close the gap to {{active_selection_cue_residual:.1e}} nats, while a pragmatic-only rule commits to an arm and leaves {{active_selection_pragmatic_residual:.3f}} nats unresolved. Finite flat-prior toy, exact. Source: `output/data/firstprinciples/active_selection_demo.json`.](../output/figures/active_selection_landscape.png){#fig:active_selection_landscape width=99% fig-alt="Two-panel figure of the expected-free-energy active-selection landscape. The left panel plots two curves against cue validity: epistemic value rising and residual gap falling, with a dashed line at the prior entropy and a note that the two curves sum to it. The right panel shows grouped bars per canonical data-collection policy (cue, commit_left, commit_right, stay) for four quantities: expected free energy, epistemic value, pragmatic value, and residual gap."}

![The analytical active-selection result predicts the project's pymdp sophisticated-inference simulation quantitatively. The curve is the closed-form residual $E_o[H(r\mid o)]$ versus cue validity; the point is the SI agent's measured post-cue belief entropy ({{si_bridge_post_cue_entropy:.3f}} nats) at the environment's own cue validity ({{si_bridge_cue_validity:.2f}}). They agree to {{si_bridge_match_abs:.1e}} nats — a quantitative, validity-specific prediction, not a fit, and the agent visits the cue before any arm. The bridge is bound to observable belief entropy because pymdp does not expose its internal expected-free-energy terms. Finite toy, exact. Source: `output/data/firstprinciples/si_bridge_demo.json`.](../output/figures/si_bridge_match.png){#fig:si_bridge_match width=80% fig-alt="Single-panel figure plotting the closed-form residual conditional entropy against cue validity as a smooth curve, with a single overlaid point marking the pymdp sophisticated- inference agent's measured post-cue belief entropy. The point lies on the curve at the environment's cue validity, with an annotation reporting the absolute difference."}

![The closed-form bridge predicts the pymdp sophisticated-inference agent's belief entropy at every step, not only post-cue. The line is the analytical running-Bayesian belief entropy over the reward-location latent (flat prior, sharpened by the cue observation, resolved by the reward); the open circles are the agent's measured belief entropy. They agree across the whole rollout to {{si_bridge_max_trajectory_error:.1e}} nats, and a wrong cue validity or shuffled observation order breaks the match. Observable bridge, finite toy. Source: `output/data/firstprinciples/si_bridge_demo.json`.](../output/figures/si_belief_entropy_trajectory.png){#fig:si_belief_entropy_trajectory width=80% fig-alt="Single-panel figure plotting belief entropy in nats against rollout step. A line shows the closed-form running-Bayesian prediction and open circles show the pymdp sophisticated- inference agent's measured belief entropy; the two coincide at every step, with an annotation giving the maximum trajectory error."}
