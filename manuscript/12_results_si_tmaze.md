# On-policy student rollout (T-maze) {#sec:results_si_tmaze}

<!-- sheaf-track:prose -->

The T-maze rollout instantiates one process-level witness for the thesis: an agent that generates its own observations and acts to minimise expected free energy, the active-inference analogue of a student policy producing the rollouts on which a teacher scores it [@agarwal2024gkd; @friston2017process; @parr2022active; @vanoostrum2024discrete_active_inference]. Sophisticated inference here is only the pymdp planner's finite beliefs-about-policies machinery; it is cited alongside self-distillation methods that condition teachers on student traces or predictive signals, but it is not evidence about those systems [@zhao2026opsd; @liu2026sdpg; @liu2026oisd]. The pymdp harness rolls out the full TMaze active-inference agent under the canonical `{{pymdp_planner}}` planner with SI search horizon {{si_tmaze_planning_horizon}} and Agent policy length {{si_tmaze_policy_len}}. Summary metrics land in `output/data/si_tmaze_summary.json`; trace-level $q_\pi$ rows, action marginals, modality observations, and tree metadata land in `output/data/si_tmaze_trace.json`.

In this correspondence privilege is operationalized as *differential cue reliability*: the cue observation plays the role of the privileged information $I$, and `cue validity` sets how reliable each agent's access to it is. In the toy world the cue is part of the rollout dynamics for every agent — what differs is access quality, a structured-partial-observation analogue (rather than a literal train-only variable removed at deployment) of a hint, verified trace, long context, visual clue, or rich textual feedback in privileged-information and self-distillation settings [@kirchhoff2018markov; @vapnik2009lupi; @lopezpaz2016unifying; @cai2024privileged_pomdp; @snell2022context_distillation; @ye2026context_distillation; @lazaridis2026edge_opd; @liu2026visual_advantage_opd; @hubotter2026sdpo; @shenfeld2026sdft]. Acting to disambiguate the cue is the epistemic component of expected free energy: the on-policy student seeks the teacher signal precisely on the novel states it reaches itself [@friston2017curiosity; @champion2024efe_unification; @sajid2021bayesian_design]. The epistemic term offers one formal lens — not the demonstrated causal mechanism — for understanding why self-generated rollouts can expose mismatch that teacher-forced evaluation may hide; it is the information-gain term that off-policy, teacher-generated data does not supply [@ross2011dagger; @sun2017aggrevated; @bengio2015scheduled; @arora2022exposure; @rohatgi2025next_token_barrier; @pozzi2025exposure_distill; @hinton2015distilling; @friston2010fep; @millidge2020efe]. Rollout transitions: {{si_tmaze_steps}}; recorded timesteps: {{si_tmaze_rollout_timestep_count}}. Mean belief entropy: {{si_tmaze_mean_belief_entropy_formatted}} nats; mean policy entropy: {{si_tmaze_policy_entropy_mean_formatted}} nats. Belief entropy over the rollout is traced in [@fig:si_belief_entropy_curve]; modality-specific observations and selected actions are in [@fig:si_obs_action_trace]. The initial selected action is `{{si_tmaze_initial_selected_action_name}}`, with cue-directed marginal probability {{si_tmaze_first_action_cue_probability:.3f}}; the cue appears at recorded timestep {{si_tmaze_cue_observed_step}}, the reward/outcome appears at timestep {{si_tmaze_reward_observed_step}}, and the extracted trace therefore records cue-before-reward ordering as {{si_tmaze_cue_before_reward}}. [@fig:si_tmaze_actions] shows how $q_\pi$ action probabilities evolve across the rollout, while the policy-entropy drop after the cue is {{si_tmaze_policy_entropy_drop_after_cue_formatted}} nats.

The policy posterior itself — the $q$ over policies that this process witness uses as the distillation-target analogue — is shown measured, step by step, in [@fig:policy_posterior_grid], drawn from all {{posterior_grid_available_count}} of {{posterior_grid_row_count}} grid rows in `output/data/pymdp_policy_posterior_grid.json`. The contrast between the two planners is the point: the sophisticated-inference posterior concentrates onto a single action within the first steps because the agent's own rollout delivers the cue observation that sharpens it, while the comparison-only vanilla evaluator remains near-uniform over its policy set for the whole horizon — it never acts on what it could learn. In this finite rollout, posterior sharpening under self-generated observations is the process-level motif: the variational posterior narrows because it generates the observations it is corrected against.

This single-agent rollout has a direct two-agent reading that we make explicit in the executable classroom demonstration: a privileged teacher with cue validity {{classroom_teacher_cue_validity}} against an on-policy student with cue validity {{classroom_student_cue_validity}} yields teacher belief entropy {{classroom_teacher_belief_entropy_formatted}} nats versus the student's {{classroom_student_belief_entropy_formatted}} nats. The measured effect is a toy posterior-sharpness gap induced by the teacher's stronger cue channel, not a prediction that a Markov blanket numerically fixes entropy in general. The advantage measured and claimed is posterior sharpness, not task success: in this {{classroom_step_count}}-decision rollout the artifact records teacher goal-reached `{{classroom_teacher_goal_reached}}` and student goal-reached `{{classroom_student_goal_reached}}` - the sharper privileged posterior did not translate into goal attainment on this short horizon, and the manuscript claims only what the entropy series measures. The mean reverse-KL distillation signal between the two is {{classroom_mean_reverse_kl_formatted}} nats - the finite toy objective that maps onto variational free energy $F = D_{\mathrm{KL}}(q\,\|\,p(s\mid o)) - \log p(o)$ (KL target the exact posterior $p(s\mid o)\propto p(o,s)$) [@gu2024minillm; @levine2018rlinference]. Entropy-aware and adaptive-exposure OPD work is therefore a design-space neighbour: it motivates why teacher uncertainty should sometimes switch a token toward mode-covering pressure or withhold unreliable supervision, while our classroom only reports the toy teacher/student entropy gap it generated [@jin2026entropy_opd; @han2026adaptive_teacher_exposure; @luo2026demystifying_opd]. The classroom artifact is also where the manuscript keeps internal self-distillation separate from external privileged-information distillation: OISD-style internal alignment [@liu2026oisd] is cited as a method-family analogue, while this figure's numbers come only from `output/data/firstprinciples/classroom.json`. A teacher cue-validity sweep turns this single comparison into a dose-response experiment with its own built-in negative control ([@fig:privilege_dose_response]). Across {{privilege_sweep_level_count}} privilege levels (student fixed at cue validity {{privilege_sweep_student_cue_validity}}), the identical-agent baseline gap is {{privilege_sweep_baseline_gap:.1f}} by construction — a wiring/fabrication check (identical configurations cannot differ), not a control for the effect itself. The belief-entropy advantage appears only at the top of the grid: the gap stays at zero through cue validity {{privilege_sweep_last_flat_validity}} and is {{privilege_sweep_top_gap:+.3f}} nats at {{privilege_sweep_top_validity}}. Whether the onset is a sharp threshold or a steep slope is resolution-limited here — the grid has a single level between {{privilege_sweep_last_flat_validity}} and {{privilege_sweep_top_validity}} — so we claim only that the advantage is strongly nonlinear in cue validity. The mean reverse-KL distillation signal is the more sensitive detector of privilege, and the claim rests on its monotone rise across the sweep rather than any single level: above a 10^-3-nat floor (set four orders of magnitude above the ~10^-7-nat rollout float noise observed at low validities), the signal first clears the floor at cue validity {{privilege_sweep_first_nonzero_validity}} ({{privilege_sweep_first_nonzero_kl:.3g}} nats) and rises monotonically to {{privilege_sweep_top_kl:.3g}} nats — on the thesis' own terms this is exactly what should happen, since the reverse-KL loss is the free-energy gradient the student would descend, and it registers privileged information that summary statistics of the posterior miss. As with everything in this section, these are deterministic toy measurements (gap/validity rank correlation {{privilege_sweep_gap_rank_correlation:.2f}}), not significance claims.

The matrix/value audit ([@fig:si_tmaze_model_matrices]) confirms {{si_tmaze_matrix_shape_summary}}, normalized A/B/D probability mass, cue validity {{si_tmaze_cue_validity}}, and reward condition {{si_tmaze_reward_condition}}. Policy-comparison rows: {{si_policy_comparison_run_count}} across canonical SI and vanilla comparison-only planners; goal-reaching rows: {{si_policy_comparison_goal_reached_count}}. Graph-world extension rows: {{si_graph_world_steps}} over {{si_graph_world_node_count}} nodes, with goal-reached flag {{si_graph_world_goal_reached}}.

Beyond the static T-maze, a family of dynamic simulators stresses each leg of the correspondence under controlled distribution shift. Their convergent behaviour is a consistency check for the declared toy objectives, not a guarantee about stochastic OPD training. The first simulator isolates the *active-sampling* leg: a generalised-knowledge-distillation (GKD) sweep that scores the same teacher signal on student-generated versus teacher-generated rollouts [@agarwal2024gkd]. Scoring the same student under its own state-visitation measure reveals {{gkd_on_policy_loss:.3f}} nats of loss, of which a teacher-visitation evaluation sees only {{gkd_off_policy_loss:.3f}} nats — an exposure gap of {{gkd_exposure_gap:.3f}} nats that off-policy evaluation hides. This is the simulator's local information-gain analogue; it is not an empirical estimate of exposure-bias severity in language models [@ross2011dagger; @sun2017aggrevated; @bengio2015scheduled; @arora2022exposure; @rohatgi2025next_token_barrier; @pozzi2025exposure_distill; @millidge2020efe]. The gap echoes the cue-disambiguation dynamics traced in [@fig:si_belief_entropy_curve]: the student must sample to be scored where it actually goes.

The second simulator isolates the *free-energy-descent* leg. Treating reverse-KL distillation as variational expectation-maximisation - E-step over the student's own rollout posterior, M-step on the generative-model parameters - the loop converges in {{em_iterations}} steps to a residual variational gap of {{em_final_gap:.3f}} nats, and the descent is monotone ({{em_monotone_descent}}), exactly the non-increasing free-energy trajectory expected under this clean E/M alternation [@friston2010fep; @friston2017process; @dacosta2020discrete; @levine2018rlinference; @fellows2018virel]. The falsifiable signature here is local and algorithmic: this exact toy E/M objective should not increase across its own updates. It is not a claim that stochastic OPD training is globally monotone under arbitrary optimizers, teacher targets, or rollout distributions.

The third simulator isolates the *risk-tilting* leg through a diversity Pass-at-k sweep, in which the temperature of the student's sampling distribution trades finite-sample sharpness against coverage - the EFE risk/ambiguity balance read off generation diversity. For independent samples, $Pass@k = 1-(1-p)^k$, so the curve reports how the probability of at least one correct sample changes as the student distribution flattens or sharpens. The flattest (high-entropy, coverage-favouring) student reaches Pass-at-k {{diversity_flattest_pass_at_k:.3f}} ([@fig:diversity_tradeoff]) versus the sharpest (low-entropy, concentrated) student at {{diversity_sharpest_pass_at_k:.3f}}, while greedy decoding bottoms out at Pass-at-1 {{diversity_greedy_pass_at_1:.3f}}; the ordering shows why a pure concentration objective can need an epistemic/coverage term in finite tasks [@gu2024minillm; @hinton2015distilling; @wu2024rethinking_kl_kd; @stanton2021kd_work; @jin2026entropy_opd]. Finally, an adaptive-divergence controller that interpolates between reverse- and forward-KL settles at a reverse-fraction of {{adaptive_reverse_fraction:.2f}}, an empirical compromise between concentration and mass-covering geometries reported separately in this manuscript and consistent with the variational/EFE blend rather than either extreme [@ko2024distillm; @ko2025distillm2; @zhu2026hpd; @penaloza2026pidistill; @parr2022active]. Each of these four simulator legs is a stylized minimal-model demonstration, not a production-scale measurement; their value is that all four move in the direction the formal correspondence predicts.

We read these results as a minimal-model demonstration of the formal correspondence between on-policy distillation and active inference, not as claims about production language models; the quantitative findings are limited to the analytical toy, the T-maze rollout, the dynamic simulators, and the classroom artifact reported here [@penaloza2026pidistill; @dacosta2020discrete].

<!-- sheaf-track:pymdp -->

Rollout trace: `output/data/si_tmaze_trace.json`. Matrix/value audit: `output/data/si_tmaze_model_matrices.json`. JSONL run log: `output/logs/pymdp_runs.jsonl`.

<!-- sheaf-track:visualization -->

![Belief entropy in nats over the course of the pymdp T-maze rollout (mean {{si_tmaze_mean_belief_entropy_formatted}} nats; mean policy entropy {{si_tmaze_policy_entropy_mean_formatted}} nats), tracing how the agent's uncertainty changes once the cue is observed at timestep {{si_tmaze_cue_observed_step}} and before the reward/outcome appears at timestep {{si_tmaze_reward_observed_step}}. The entropy trace is the epistemic payoff of the cue action made quantitative.](../output/figures/si_belief_entropy_curve.png){#fig:si_belief_entropy_curve width=90% fig-alt="Line plot of belief entropy in nats versus timestep for the pymdp T-maze rollout. Entropy ranges from {{si_entropy_min}} to {{si_entropy_max}} nats across {{si_tmaze_rollout_timestep_count}} rollout records under the canonical {{pymdp_planner}} planner."}

![Multimodal observation and action traces for the full TMaze rollout ({{si_tmaze_observation_modality_count}} observation modalities; action diversity {{si_action_diversity}}). Upper: location, outcome, and cue observation indices over time. Lower: the selected action index and names. Together the panels show the closed perception--action loop that produces the teacher's behavior: cue observation at timestep {{si_tmaze_cue_observed_step}}, reward/outcome observation at timestep {{si_tmaze_reward_observed_step}}, and cue-before-reward ordering {{si_tmaze_cue_before_reward}}. Capturing this joint observation--action structure, rather than a marginal action policy, is what makes the active-inference teacher faithfully reproducible.](../output/figures/si_obs_action_trace.png){#fig:si_obs_action_trace width=90% fig-alt="Dual-panel plot of the full pymdp TMaze rollout. The upper panel overlays location, outcome, and cue observation modality indices; the lower panel shows the selected action index and action names. Goal reached flag is {{si_goal_reached}}."}

![Canonical sophisticated-inference action selection for the full pymdp TMaze rollout (agent policy length {{si_tmaze_policy_len}}, SI search horizon {{si_tmaze_planning_horizon}}). Left: selected action index per timestep. Right: first-action marginals from the policy posterior over all five location actions. The initial selected action is {{si_tmaze_initial_selected_action_name}}, with cue-directed marginal probability {{si_tmaze_first_action_cue_probability:.3f}}. The policy-entropy drop after the cue is {{si_tmaze_policy_entropy_drop_after_cue_formatted}} nats, quantifying the information-seeking step an on-policy student must learn to reproduce.](../output/figures/si_tmaze_actions.png){#fig:si_tmaze_actions width=90% fig-alt="Two-panel plot for the canonical pymdp full TMaze sophisticated-inference rollout. Left panel shows selected action index by timestep; right panel shows marginal first-action probabilities from q-pi for all five location actions. The initial selected action is {{si_tmaze_initial_selected_action_name}} under SI search horizon {{si_tmaze_planning_horizon}}."}

![Two-agent classroom distillation signal over {{classroom_step_count}} decision points (3 transitions) between a privileged teacher (cue validity {{classroom_teacher_cue_validity}}) and an on-policy student (cue validity {{classroom_student_cue_validity}}). Left: per-step reverse KL, forward KL, and Jensen-Shannon divergence between the teacher and student policies (mean reverse KL {{classroom_mean_reverse_kl_formatted}} nats, mean Jensen-Shannon {{classroom_mean_jensen_shannon:.3f}} nats, {{classroom_agreement_count}} agreement rows). Right: a heatmap of teacher-minus-student action probability by action and step, localizing where the student diverges. The figure operationalizes on-policy distillation as a per-step divergence signal evaluated along the student's own trajectory, the same active-inference principle of scoring beliefs on visited states rather than the teacher's idealized path.](../output/figures/classroom_distillation_signal.png){#fig:classroom_distillation_signal width=94% fig-alt="Two-panel classroom distillation figure. The left panel plots per-step reverse KL, forward KL, and Jensen-Shannon divergence between the privileged teacher and on-policy student policies. The right panel is a heatmap of teacher probability minus student probability by action and classroom step."}

*See [@fig:si_tmaze_model_matrices] (Full TMaze generative-model matrix and value audit.)*

![Teacher-privilege dose-response over {{privilege_sweep_level_count}} cue-validity levels (student fixed at {{privilege_sweep_student_cue_validity}}; the identical-agent baseline gap {{privilege_sweep_baseline_gap:.1f}} is a wiring check, not an effect control). The belief-entropy advantage is strongly nonlinear: zero through cue validity {{privilege_sweep_last_flat_validity}}, {{privilege_sweep_top_gap:+.3f}} nats at {{privilege_sweep_top_validity}} (step-versus-slope is resolution-limited by the grid). The mean reverse-KL distillation signal is the more sensitive detector, rising from its first appreciable value ({{privilege_sweep_first_nonzero_kl:.3g}} nats above a 10^-3-nat noise floor) at cue validity {{privilege_sweep_first_nonzero_validity}} to {{privilege_sweep_top_kl:.3g}} nats — the loss the student would descend registers privilege that posterior-entropy summaries miss. Gap/validity rank correlation {{privilege_sweep_gap_rank_correlation:.2f}}. Source: `output/data/firstprinciples/privilege_sweep.json`.](../output/figures/privilege_dose_response.png){#fig:privilege_dose_response width=95% fig-alt="Two-panel dose-response figure from the deterministic classroom sweep over teacher cue validity. Left panel: belief-entropy gap (student minus teacher) is exactly zero through cue validity {{privilege_sweep_last_flat_validity}} and jumps at the highest level. Right panel: mean reverse-KL distillation signal on a logarithmic axis turns on earlier and grows by orders of magnitude."}

![The student's measured posterior over policies, step by step — the quantity the correspondence identifies with the distillation target. Left and center: marginal action posteriors per rollout step for both planners (cell values are probabilities; labels shown for mass at or above 0.30). Right: policy-posterior entropy by step for all {{posterior_grid_available_count}} of {{posterior_grid_row_count}} measured grid rows. Entropy collapses after the cue observation: the on-policy rollout is what exposes the agent to the observation that sharpens its own posterior, the T-maze version of the induced-distribution argument for on-policy distillation. Source: `output/data/pymdp_policy_posterior_grid.json`.](../output/figures/policy_posterior_grid.png){#fig:policy_posterior_grid width=99% fig-alt="Three-panel figure of measured policy posteriors from the pymdp T-maze harness. The first two panels are step-by-action heatmaps of the marginal action posterior for the sophisticated-inference and vanilla planners; the third panel plots policy-posterior entropy in nats against rollout step for both planners."}
